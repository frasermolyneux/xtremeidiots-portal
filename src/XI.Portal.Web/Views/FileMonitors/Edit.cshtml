@model XI.Portal.Data.Legacy.Models.FileMonitors

@{
    ViewData["Title"] = "Edit File Monitor";
}

<row class="row">
    <div class="container-fluid">
        <div class="card card-header">
            @ViewData["Title"]
        </div>
        <div class="card card-body">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="FileMonitorId"/>
                <div class="form-group">
                    <label asp-for="GameServerServerId" class="control-label"></label>
                    <select asp-for="GameServerServerId" class="form-control" asp-items="ViewBag.GameServerServerId" disabled="disabled"></select>
                    <span asp-validation-for="GameServerServerId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="FilePath" class="control-label"></label>
                    <input asp-for="FilePath" class="form-control"/>
                    <span asp-validation-for="FilePath" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary"/>
                </div>
            </form>
        </div>
        <div class="card card-footer">
            <a asp-action="Index">Back to List</a>
        </div>
    </div>
</row>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
@model List<string>

@{
    Layout = null;
}

<!DOCTYPE html>

<table style="margin: auto">
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td style="padding: 2px;">
                <div style="text-align: center;">
                    @Html.Raw(item)
                </div>
            </td>
        </tr>
    }
    </tbody>
</table>